<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.soc.shopmanagmentsoc">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShopManagmentSOC"
        android:usesCleartextTraffic="true">
        <activity android:name=".ChangePassword"></activity>
        <activity android:name=".Profile" />
        <activity android:name=".shopuser.ShopUserHome" />
        <activity android:name=".adminpart.AdminUserHome" />
        <activity android:name=".MainActivity" />
        <activity android:name=".adminpart.DashBordProscessData" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>